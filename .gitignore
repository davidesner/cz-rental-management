node_modules/
dist/
data/
.env
.env.local
.mcp.json
rental_backups/
*.log
.DS_Store
coverage/
.vscode/
*.tgz

# Claude Code — locally installed skills + their lockfile
.claude/skills/
/skills-lock.json
# Git worktrees created inside the repo; `git add -A` would otherwise commit a
# whole nested checkout.
.claude/worktrees/
