node_modules/
dist/
data/
.env
.env.local
.mcp.json
rental_backups/
*.log
.DS_Store
coverage/
.vscode/
*.tgz

# Claude Code — per-machine state only: locally installed skills, local
# permission overrides, and worktrees created inside the repo (a nested
# checkout `git add -A` would otherwise commit whole).
.claude/
/skills-lock.json
.vercel
.env*.local
test-results/

# Python (skill scripts)
__pycache__/
*.pyc
